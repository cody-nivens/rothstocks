<%= datagrid_form_for @grid, :method => :get, :url => portfolios_path %>
<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<%= link_to 'New Portfolio', new_portfolio_path %>

