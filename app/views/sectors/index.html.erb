<%#= datagrid_form_for @grid, :method => :get, :url => sectors_path %>

<%= bar_chart @chart_list, stacked: true %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>
