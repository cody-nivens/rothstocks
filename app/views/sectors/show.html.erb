<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @sector.name %>
</p>

<div id="main-div">
<%#= datagrid_form_for @industries_grid, :method => :get, :url => industries_path %>

<div id="grid-div">
<%#= paginate(@industries_grid.assets) %>
<%= datagrid_table @industries_grid %>
<%#= paginate(@industries_grid.assets) %>
</div>
<div id="bar-chart-div">
<%= bar_chart @chart_list, stacked: true %>
</div>
</div>

<div class="clear"></div>

<%= datagrid_form_for @grid, :method => :get, :url => stocks_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<%= link_to 'Edit', edit_sector_path(@sector) %> |
<%= link_to 'Back', sectors_path %>
